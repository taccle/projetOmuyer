#include "calculs.hpp"



int distance(int rayon, vector<int>* dist){
  if(rayon){
    //on a un rayon
    cout << '\a';
    (*dist).push_back(rayon);
  }
  else{
    // pas de rayon

  }

  return 0;
}
